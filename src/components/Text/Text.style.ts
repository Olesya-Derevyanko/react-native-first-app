import { StyleSheet } from 'react-native';

const styles = StyleSheet.create({
  header: {
    color: 'black',
    fontSize: 30,
    fontWeight: '900',
    alignSelf: 'center',
  },
  text: {
    color: 'black',
    fontSize: 15,
    fontWeight: '500',
  },
});

export default styles;
